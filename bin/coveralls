#!/bin/bash

# Atajo para ejecutar un comando dentro del container del server de dev

exec $(dirname "$0")/dev-compose exec -e COVERALLS_REPO_TOKEN=$COVERALLS_REPO_TOKEN hello_flask sh -c coveralls "$@"

